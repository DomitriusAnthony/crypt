<%= form_for(@post) do |f| %>
	<div class="field">
		<%= f.text_area :body, placeholder: "What's on your mind?", class: "status-box" ,rows: "3" %>
		<br />
		<%= f.submit "Post", class: 'btn btn-primary' %>
	</div>

<% end %>